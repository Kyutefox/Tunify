/// Supabase project credentials. Override via [supabase_prefs] for per-user custom endpoints.
abstract final class SupabaseConfig {
  static const String url = '';
  static const String anonKey = '';
}
