/// Minimum scroll extents for onboarding-style pages that use [Spacer]s inside a
/// scroll view. Values are layout-only (not visual tokens).
abstract final class OnboardingLayoutSizing {
  static const double guestProfileMinHeight = 620.0;
}
