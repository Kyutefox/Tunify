name: tunify_logger
description: Beautiful terminal logger for Tunify (wraps the logger package).
version: 1.0.0
publish_to: "none"

environment:
  sdk: ^3.6.0

dependencies:
  logger: ^2.6.2

dev_dependencies:
  lints: ^6.0.0
