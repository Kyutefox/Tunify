name: tunify_database
description: SQLite-first database bridge for Tunify.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.6.0

dependencies:
  path: ^1.9.0
  path_provider: ^2.1.2
  sqflite: ^2.3.2

dev_dependencies:
  lints: ^6.0.0
